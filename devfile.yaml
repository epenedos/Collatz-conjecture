schemaVersion: 2.1.0
metadata:
  name: collatz
components:
  - name: frontend
    container:
      image: registry.redhat.io/devspaces/udi-rhel8:3.1
      memoryLimit: 3Gi
      mountSources: true
      env:
        - name: GOPATH
          # replicate the GOPATH from the plugin
          value: /projects/.che/gopath:/projects
        - name: GOCACHE
          # replicate the GOCACHE from the plugin, even though the cache is not shared
          # between the two
          value: /tmp/.cache
      endpoints:
        - exposure: public
          name: 'collatzfe'
          protocol: http
          targetPort: 8080
  - name: backend
    container:
      image: registry.redhat.io/devspaces/udi-rhel8:3.1
      memoryLimit: 3Gi
      mountSources: true
      env:
        - name: GOPATH
          # replicate the GOPATH from the plugin
          value: /projects/.che/gopath:/projects
        - name: GOCACHE
          # replicate the GOCACHE from the plugin, even though the cache is not shared
          # between the two
          value: /tmp/.cache
      endpoints:
        - exposure: public
          name: 'collatzbe'
          protocol: http
          targetPort: 8081
          path: collatz-be
commands:
  - id: build-fe
    exec:
      component: frontend
      workingDir: ${PROJECTS_ROOT}/collatz-conjecture/frontend
      commandLine: go build collatz-fe
      group:
        kind: build
  - id: run-fe
    exec:
      component: frontend
      workingDir: ${PROJECTS_ROOT}/collatz-conjecture/frontend
      commandLine: ./collatz-fe
      group:
        kind: run
  - id: build-be
    exec:
      component: backend
      workingDir: ${PROJECTS_ROOT}/collatz-conjecture/backend
      commandLine: go mod tidy; go build collatz-be
      group:
        kind: build
  - id: run-be
    exec:
      component: backend
      workingDir: ${PROJECTS_ROOT}/collatz-conjecture/backend
      commandLine: ./collatz-be
      group:
        kind: run